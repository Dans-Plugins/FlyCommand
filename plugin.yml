main: me.Daniel.FlyCommand.Main
name: FlyCommand
version: 1.0
api-version: 1.13

commands:
  Fly:
    description: Toggles flight mode on or off for the executing player.
    usage: /<command>

permissions:
  FlyCommand.fly:
    description: Allows the player to toggle flight.
    default: op
